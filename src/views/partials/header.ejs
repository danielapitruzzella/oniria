<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle + ' — Oniria' : 'Oniria' %></title>
  <link href="/css/output.css" rel="stylesheet">
  <link rel="icon" href="/archivio-sogni.png" type="image/png">
  <% if (typeof pageScript !== 'undefined' && pageScript) { %>
    <script src="/js/<%= pageScript %>" defer></script>
  <% } %>
</head>
<body class="bg-night min-h-screen flex flex-col text-gray-800">
